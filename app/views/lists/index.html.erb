<h1>Here are all of your lists!</h1>

<ul>
  <% @lists.each do |list| %>
    <%= link_to list_path(list.id) do %>
      <li><%= list.name %></li>
    <% end %>
  <% end %>
</ul>

<p>
<%= link_to "Create a new list", new_list_path %>
</p>